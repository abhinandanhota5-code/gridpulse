{
  "name": "gridpulse-backend",
  "version": "1.0.0",
  "description": "GRIDPULSE API — serves driver and fleet-owner dashboard data. Deploy target: Render.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "modbus-serial": "^8.0.25",
    "ocpp-ws-io": "^2.3.1"
  }
}
